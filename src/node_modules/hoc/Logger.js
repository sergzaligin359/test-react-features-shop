import React, { Component } from 'react'

export default function log(WrappedComponent){

    class Logger extends Component {
        state = {
            message: 'logger message'
        };

        render() {
            return <WrappedComponent { ...this.props }  message={ this.state } />
        }
    }

    return Logger;

}

