import { phones } from 'api/mock/phones';

export const fetchPhones = async () => {
    return new Promise((resolve, reject) => {
        if(phones.length){
            resolve(phones);
        }else{
            reject({
                message: 'Phones not load',
                type: 'api request error'
            })
        }
    });
};