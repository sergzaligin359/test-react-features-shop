import { prop, map } from 'utils';

export const getPhoneById = (state, id) => prop(id, state.phones.phones);

export const getPhones = state => {
    const phones = map(state.phonesPage.ids, id => getPhoneById(state, id));
    //console.log('id', phones);
    return phones;
}